<!doctype html>
<html lang="en">

<head>
  <title><%= user.username %>’s Profile</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

  <!-- Bootstrap CSS v5.3.2 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />

  <style>
    body {
      background-color: #f8f9fa;
    }
    .profile-header {
      background: linear-gradient(135deg, #6c63ff, #4e54c8);
      color: white;
      border-radius: 12px;
      padding: 2rem;
      margin-bottom: 2rem;
    }
    .post-card {
      border-radius: 12px;
      transition: transform .2s ease, box-shadow .2s ease;
    }
    .post-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 6px 16px rgba(0,0,0,0.15);
    }
  </style>
</head>

<body>
  <main>
    <div class="container mt-4">

      <!-- Profile Header -->
      <div class="profile-header text-center shadow-sm">
        <img src="https://i.pravatar.cc/100?u=<%= user.username %>" class="rounded-circle mb-3 border border-light"
          alt="profile">
        <h2 class="mb-0"><%= user.username %></h2>
        <small class="text-light">Member since Nov 22, 2022</small>
      </div>

      <!-- Posts Section -->
      <h4 class="mb-3">Your Posts</h4>
      <% if (posts.length > 0) { %>
        <div class="row g-3">
          <% posts.forEach(p => { %>
            <div class="col-md-6">
              <div class="card post-card p-3 shadow-sm">
                <h5>
                  <a href="/posts/<%= p._id %>" class="text-decoration-none text-dark">
                    <%= p.title %>
                  </a>
                </h5>
                <div class="d-flex justify-content-between align-items-center mt-2">
                  <div>
                    <a href="/posts/<%= p._id %>/edit" class="btn btn-outline-secondary btn-sm">✏️ Edit</a>
                    <form method="POST" action="/posts/<%= p._id %>/delete" class="d-inline">
                      <button class="btn btn-outline-danger btn-sm">🗑 Delete</button>
                    </form>
                  </div>
                  <small class="text-muted">Updated: <%= p.updatedAt.toDateString() %></small>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="text-muted">You haven’t published anything yet.</p>
      <% } %>

    </div>
  </main>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r"
    crossorigin="anonymous"></script>
</body>

</html>
