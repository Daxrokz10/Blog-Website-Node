<!doctype html>
<html lang="en">

<head>
    <title>Delete User</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />

    <style>
        body {
            background-color: black;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .form-container {
            max-width: 700px;
            margin: 50px auto;
            background-color: #001925;
            padding: 30px;
            border-left: 5px solid #f43838;
            clip-path: polygon(0 0,
                    100% 0,
                    100% calc(100% - 20px),
                    calc(100% - 20px) 100%,
                    0 100%);
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.6);
        }

        .heading {
            display: block;
            color: white;
            font-size: 1.5rem;
            font-weight: 800;
            margin-bottom: 20px;
        }

        .c1 {
            display: block;
            color: #f43838;
            font-size: 1rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .c2 {
            display: block;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .input {
            color: #87a4b6;
            width: 100%;
            background-color: #002733;
            border: none;
            outline: none;
            padding: 10px;
            margin-bottom: 20px;
            font-weight: bold;
            border-left: 1px solid transparent;
            transition: all 0.2s ease-in-out;
        }

        .input:focus {
            border-left: 5px solid #f43838;
        }

        .button-container {
            display: flex;
            gap: 10px;
        }

        .send-button {
            flex-basis: 70%;
            background: #f43838;
            padding: 10px;
            color: #001925;
            text-align: center;
            font-weight: bold;
            border: 1px solid transparent;
            transition: all 0.2s ease-in-out;
            cursor: pointer;
        }

        .send-button:hover {
            background: transparent;
            border: 1px solid #ff7a01;
            color: #f43838;
        }

        .reset-button-container {
            flex-basis: 30%;
        }

        .reset-button {
            display: block;
            text-align: center;
            padding: 10px;
            color: #f43838;
            font-weight: bold;
            background: #001925;
            clip-path: polygon(0 0,
                    100% 0,
                    100% calc(100% - 10px),
                    calc(100% - 10px) 100%,
                    0 100%);
            transition: all 0.2s ease-in-out;
            text-decoration: none;
        }

        .reset-button:hover {
            background: #f43838;
            color: #001925;
        }
    </style>
</head>

<body>
    <main>
        <div class="form-container">
            <div class="form">
                <span class="heading">Are you sure you want to delete your account?</span>
                <span class="c1">Enter your password to confirm</span>

                <form action="/deleteUser" method="POST">
                    <input class="input" type="password" name="password" placeholder="Password" required />

                    <span class="c2">⚠️ Once your profile is deleted, it cannot be recovered!</span>

                    <div class="button-container mt-3">
                        <button type="submit" class="send-button">Delete Profile</button>
                        <div class="reset-button-container">
                            <a class="reset-button" href="/profile">Back to Profile</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4"
        crossorigin="anonymous"></script>
</body>

</html>
